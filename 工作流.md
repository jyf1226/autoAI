在当前目录帮我创建一个 Windows + WSL2 + Docker Compose 的本地 AI 工作流项目。

目标：

1. 用 Docker Compose 管理这些服务：
  - open-webui
  - qdrant
  - postgres
  - redis
  - n8n
  - github-watch（我自己的 Python 服务）
2. Ollama 不放进容器，默认跑在宿主机。
3. 所有数据都使用 bind mounts，统一挂载到 ./data 下。
4. 所有配置都可迁移，未来换电脑时，我只需要复制当前目录并重新 docker compose up -d。

目录结构要求：
.
├─ compose/
│  ├─ docker-compose.yml
│  ├─ .env.example
│  └─ README.md
├─ data/
│  ├─ openwebui/
│  ├─ qdrant/
│  ├─ postgres/
│  ├─ redis/
│  ├─ n8n/
│  └─ github-watch/
├─ github-watch/
│  ├─ app/
│  │  ├─ main.py
│  │  ├─ fetch_github.py
│  │  ├─ normalize_events.py
│  │  ├─ summarize_with_ollama.py
│  │  └─ build_daily_report.py
│  ├─ requirements.txt
│  ├─ Dockerfile
│  └─ README.md
└─ scripts/
   ├─ start.ps1
   ├─ stop.ps1
   └─ backup.ps1

github-watch 服务要求：

1. 从 config/repos.yaml 读取仓库列表
2. 用 GitHub REST API 拉取最近 24 小时的 commits / pull requests / issues
3. 保存原始 JSON 到 data/github-watch/raw/
4. 生成规范化 JSON 到 data/github-watch/normalized/
5. 调用宿主机 Ollama HTTP API 生成中文总结
6. 生成日报 Markdown 到 data/github-watch/reports/
7. 代码要有日志、异常处理、重试和基础速率限制处理
8. 所有环境变量从 .env 读取
9. README 里写清楚 Windows + Docker Desktop + WSL2 的启动步骤

Docker Compose 要求：

1. 使用官方/常用镜像
2. 服务之间网络互通
3. 使用 depends_on、healthcheck、restart 策略
4. 所有关键数据都持久化到 bind mounts
5. 给 open-webui 配置连接宿主机 Ollama 的方法
6. 预留将来接入 AnythingLLM 的位置，但当前先不加
7. 不要写死绝对路径，全部用相对路径和环境变量

输出要求：

1. 直接创建所有文件
2. 给出我第一天启动这套系统的命令
3. 给出一个最小可运行示例
4. 给出未来迁移到新电脑时的步骤
5. 如果有不确定的镜像或端口，优先选保守稳定方案并在 README 说明

实现风格要求：

- 先做 MVP，不要过度设计
- Python 代码清晰、模块化
- 所有脚本可单独运行也可被主程序调用
- 输出日志尽量友好
- 配置文件和代码分离
- 为未来接入 Qdrant 检索和训练样本生成预留接口

完成后请自查：

1. docker-compose.yml 是否能通过基本校验
2. 路径挂载是否统一
3. README 是否足够让我在一台全新 Windows 机器上复现
4. PowerShell 脚本是否能直接启动和停止整套服务